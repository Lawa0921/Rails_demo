<h1>餐點列表</h1>

<%= link_to "新增", new_item_path %>

<ul>
<% @items.each do |item|%>
  <li>
    <%= item.name %>
    <%= link_to "刪除", item_path(item.id), data: {confirm: '確定刪除?', method: 'delete'} %>
  </li>
<% end %>
</ul>
