<h1>
  購物車
  <%= link_to "清空購物車", cart_path, class: "btn btn-success btn-sm", method: 'delete' if not current_cart.empty? %>
</h1>

<%= render 'cart'%>

<%= link_to "繼續選購", root_path, class: "btn btn-primary"%>
<%= link_to "結帳", checkout_cart_path, class: "btn btn-danger" if not current_cart.empty? %>
